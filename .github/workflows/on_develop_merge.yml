name: Pipeline that executes jobs against QA.
on:
  push:
    branches:
      - develop
jobs:
  send_to_s3: 
    uses: diegodurli/gha/.github/workflows/upload_to_s3.yml@main
    secrets:
      assumed_role: ${{ secrets.AWS_ASSUMED_ROLE_DEV1 }}
      destination_bucket: ${{ secrets.AWS_DESTINATION_BUCKET_DEV1 }}
    with:
      aws_region: us-east-1
      file_path: ./config/file.json